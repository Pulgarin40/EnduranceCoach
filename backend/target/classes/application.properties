spring.application.name=backend

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/tfm_db
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Desactivar la autoconfiguración de Spring AI hasta que tengamos la API Key
spring.ai.openai.api-key=dummy-key-for-now
spring.ai.openai.chat.enabled=false

# Spring AI (Optional - configured to use OpenAI by default via starter, requires API key in env usually)
# spring.ai.openai.api-key=${OPENAI_API_KEY}

# Seguridad JWT
security.jwt.secret-key=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
security.jwt.expiration-time=86400000